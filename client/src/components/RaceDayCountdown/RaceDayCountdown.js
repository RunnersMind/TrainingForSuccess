import React from 'react';
import { Container } from 'reactstrap';
import './RaceDayCountdown.css';

const RaceDayCountdown = (props) => {
    return (
<Container className="race-day" fluid>
    </Container>

    );
};

export default RaceDayCountdown ;