import React from 'react';
import { Container } from 'reactstrap';
import './WeatherWidget.css';

const WeatherWidget = (props) => {
    return (
<div>
<Container className="weather" fluid>
    </Container>
    </div>

    );
};

export default WeatherWidget;